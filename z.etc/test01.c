#include <stdio.h>

int main()
    {
  int x,y;
  unsigned long long fac=1;
  scanf("%d",&x);
  {if(x<0)
    {printf("Invalid value");}
  else{
  for(x=1;x<=y;++x)
fac*=x;
}
}
return 0;}
